import styles from './Recs.module.css'

export default function Recs() {
	return (
		<div className={ styles.container }>
			<div className={ styles.header }>
				<span className={ styles.header__title }>Recommendations</span>
			</div>
		</div>
	)
}
